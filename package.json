{
  "name": "fortune-redis",
  "author": {
    "email": "contact@thibremy.ninja",
    "url": "http://thibremy.ninja"
  },
  "license": "MIT",
  "version": "1.0.0",
  "description": "Redis adapter for fortune",
  "main": "./dist/index.js",
  "scripts": {
    "clean": "rimraf dist",
    "build": "mkdirp dist && babel lib --optional runtime --out-dir dist",
    "lint": "eslint lib",
    "prepublish": "npm run lint && npm run clean && npm run build",
    "test": "npm run lint; babel-tape-runner node test | tap-dot "
  },
  "dependencies": {
    "babel-runtime": "^5.8.25",
    "co-redis": "^2.0.0"
  },
  "devDependencies": {
    "babel": "^5.8.23",
    "babel-eslint": "^4.1.3",
    "babel-tape-runner": "^1.2.0",
    "eslint": "^1.7.3",
    "eslint-config-airbnb": "^0.1.0",
    "fakeredis": "^0.3.4",
    "fortune": "^1.3.5",
    "mkdirp": "^0.5.1",
    "rimraf": "^2.4.3",
    "tap-dot": "^1.0.0"
  },
  "files": [
    "dist/",
    "lib/",
    "LICENSE"
  ],
  "babel": {
    "nonStandard": false,
    "optional": [
      "runtime"
    ]
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/thibremy/fortune-redis.git"
  },
  "keywords": [
    "fortune",
    "redis",
    "adapter"
  ],
  "engines": {
    "node": ">=0.12"
  },
}
